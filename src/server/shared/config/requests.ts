export const WINDOW_SIZE = 15 * 60 * 1000; // eslint-disable-line @typescript-eslint/no-magic-numbers

// maximal 100 requests pro IP in dem oben definierten Zeitfenster
export const MAX_REQUESTS_PER_WINDOW = 100;
